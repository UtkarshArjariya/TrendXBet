module.exports = {
    // Admins (ensure the Telegram user ID matches)
    adminTelegramId: '@parasfix',
  
    // Bet limits (adjustable for MVP)
    minBet: 0.01,
    maxBet: 1,
  
    // Solana configuration
    solanaRpcUrl: 'https://api.devnet.solana.com', // Change to mainnet for live
  };
  